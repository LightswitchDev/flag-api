# Migration `watch-20200121230652`

This migration has been generated by daaasbu at 1/21/2020, 11:07:04 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Organization" (
  "id" text  NOT NULL  ,
  "name" text    ,
  PRIMARY KEY ("id")
);

CREATE TABLE "public"."Environment" (
  "id" text  NOT NULL  ,
  "name" text  NOT NULL DEFAULT '' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "public"."Key" (
  "createdAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "id" text  NOT NULL  ,
  PRIMARY KEY ("id")
);

CREATE TABLE "public"."Variant" (
  "id" text  NOT NULL  ,
  "value" text  NOT NULL DEFAULT '' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "public"."Switch" (
  "createdAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "enabled" boolean  NOT NULL DEFAULT false ,
  "id" text  NOT NULL  ,
  "key" text  NOT NULL DEFAULT '' ,
  "name" text  NOT NULL DEFAULT '' ,
  "type" text  NOT NULL DEFAULT 'Boolean' ,
  "updatedAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY ("id")
);

ALTER TABLE "public"."Environment" ADD COLUMN "organization" text  NOT NULL  REFERENCES "public"."Organization"("id") ON DELETE RESTRICT,
ADD COLUMN "switch" text    REFERENCES "public"."Switch"("id") ON DELETE SET NULL;

ALTER TABLE "public"."Key" ADD COLUMN "environment" text    REFERENCES "public"."Environment"("id") ON DELETE SET NULL,
ADD COLUMN "organization" text    REFERENCES "public"."Organization"("id") ON DELETE SET NULL;

ALTER TABLE "public"."Variant" ADD COLUMN "switch" text    REFERENCES "public"."Switch"("id") ON DELETE SET NULL;

ALTER TABLE "public"."Switch" ADD COLUMN "defaultVariant" text    REFERENCES "public"."Variant"("id") ON DELETE SET NULL,
ADD COLUMN "organization" text  NOT NULL  REFERENCES "public"."Organization"("id") ON DELETE RESTRICT;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration watch-20200119233131..watch-20200121230652
--- datamodel.dml
+++ datamodel.dml
@@ -3,15 +3,14 @@
 }
 datasource db {
     provider = "postgresql"
-    url = "***"
+    url      = env("POSTGRESQL_URL")
 }
 model Organization {
     id   String  @default(cuid()) @id
     name String?
-
     keys Key[]
 }
 model Environment {
```


