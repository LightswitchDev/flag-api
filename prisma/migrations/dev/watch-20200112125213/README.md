# Migration `watch-20200112125213`

This migration has been generated by daaasbu at 1/12/2020, 12:52:13 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Organization" (
  "id" text  NOT NULL  ,
  "name" text  NOT NULL DEFAULT '' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "public"."Environment" (
  "id" text  NOT NULL  ,
  "name" text  NOT NULL DEFAULT '' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "public"."Switch" (
  "createdAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "id" text  NOT NULL  ,
  "type" text  NOT NULL DEFAULT 'Boolean' ,
  "updatedAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY ("id")
);

ALTER TABLE "public"."Environment" ADD COLUMN "organization" text  NOT NULL  REFERENCES "public"."Organization"("id") ON DELETE RESTRICT;

ALTER TABLE "public"."Switch" ADD COLUMN "environment" text  NOT NULL  REFERENCES "public"."Environment"("id") ON DELETE RESTRICT,
ADD COLUMN "organization" text  NOT NULL  REFERENCES "public"."Organization"("id") ON DELETE RESTRICT;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration watch-20200112004135..watch-20200112125213
--- datamodel.dml
+++ datamodel.dml
@@ -1,25 +1,34 @@
 generator photon {
-  provider = "photonjs"
+    provider = "photonjs"
 }
 datasource db {
-  provider = "postgresql"
-  url = "***"
+    provider = "postgresql"
+    url      = "postgresql://postgres:mypassword@localhost/lightswitch?sslmode=prefer"
 }
-model User {
-  id    String  @default(cuid()) @id
-  email String  @unique
-  name  String?
-  posts Post[]
+model Organization {
+    id   String @default(cuid()) @id
+    name String
 }
-model Post {
-  id        String   @default(cuid()) @id
-  createdAt DateTime @default(now())
-  updatedAt DateTime @updatedAt
-  published Boolean  @default(false)
-  title     String
-  content   String?
-  author    User?
+model Environment {
+    id           String       @default(cuid()) @id
+    organization Organization
+    name         String
+}
+
+model Switch {
+    id           String       @default(cuid()) @id
+    type         SwitchType
+    environment  Environment
+    organization Organization
+    createdAt    DateTime     @default(now())
+    updatedAt    DateTime     @updatedAt
+
+}
+
+enum SwitchType {
+    Boolean
+    Multi
 }
```


